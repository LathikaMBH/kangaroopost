[build]
  command = "npm run build"
  publish = "dist"

# single-page app: every path serves index.html (the API is called directly via VITE_API_URL)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
